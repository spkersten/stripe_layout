name: example
description: Example for stripe_layout

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stripe_layout:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
